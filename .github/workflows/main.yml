name: Build TWRP - Galaxy Tab Active3

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Free disk space
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            bc bison build-essential ccache curl flex g++-multilib \
            gcc-multilib git gnupg gperf imagemagick lib32ncurses5-dev \
            lib32readline-dev lib32z1-dev libelf-dev liblz4-tool \
            libncurses5-dev libsdl1.2-dev libssl-dev libxml2-utils \
            lzop pngcrush rsync schedtool squashfs-tools xsltproc \
            zip zlib1g-dev python3 python-is-python3

      - name: Setup repo tool
        run: |
          mkdir -p "$HOME/bin"
          curl -L https://storage.googleapis.com/git-repo-downloads/repo \
            -o "$HOME/bin/repo"
          chmod a+x "$HOME/bin/repo"
          echo "$HOME/bin" >> "$GITHUB_PATH"

      - name: Initialize TWRP source
        run: |
          mkdir -p "$GITHUB_WORKSPACE/twrp"
          cd "$GITHUB_WORKSPACE/twrp"

          repo init \
            -u https://github.com/minimal-manifest-twrp/platform_manifest_twrp_aosp.git \
            -b twrp-11 \
            --depth=1

          repo sync -c -j"$(nproc --all)" --force-sync --no-clone-bundle --no-tags

      - name: Clone Galaxy Tab Active3 device tree
        run: |
          cd "$GITHUB_WORKSPACE/twrp"

          rm -rf device/samsung/gtactive3
          git clone --depth=1 \
            https://github.com/jimenez15690-dot/twrp_gtactive3.git \
            device/samsung/gtactive3

      - name: Prepare device tree
        run: |
          cd "$GITHUB_WORKSPACE/twrp"
          DEV_DIR="device/samsung/gtactive3"

          echo "=== Device tree structure ==="
          ls -la "$DEV_DIR"

          # 1. Basis omni_gtactive3.mk sicherstellen
          if [ ! -f "$DEV_DIR/omni_gtactive3.mk" ]; then
            cat > "$DEV_DIR/omni_gtactive3.mk" <<'EOF'
          $(call inherit-product, $(SRC_TARGET_DIR)/product/full_base_telephony.mk)
          $(if $(wildcard device/samsung/gtactive3/device.mk),$(call inherit-product, device/samsung/gtactive3/device.mk))

          PRODUCT_NAME := omni_gtactive3
          PRODUCT_DEVICE := gtactive3
          PRODUCT_BRAND := samsung
          PRODUCT_MODEL := Galaxy Tab Active3
          PRODUCT_MANUFACTURER := samsung
          EOF
          else
            sed -i '/PRODUCT_NAME/d' "$DEV_DIR/omni_gtactive3.mk"
            sed -i '/PRODUCT_DEVICE/d' "$DEV_DIR/omni_gtactive3.mk"
            echo "PRODUCT_NAME := omni_gtactive3" >> "$DEV_DIR/omni_gtactive3.mk"
            echo "PRODUCT_DEVICE := gtactive3" >> "$DEV_DIR/omni_gtactive3.mk"
          fi

          # 2. twrp_gtactive3.mk als Alias mit eigener PRODUCT_NAME anlegen
          cp "$DEV_DIR/omni_gtactive3.mk" "$DEV_DIR/twrp_gtactive3.mk"
          sed -i 's/PRODUCT_NAME := omni_gtactive3/PRODUCT_NAME := twrp_gtactive3/' "$DEV_DIR/twrp_gtactive3.mk"

          # 3. AndroidProducts.mk neu schreiben und beide Produkte registrieren
          cat > "$DEV_DIR/AndroidProducts.mk" <<'EOF'
          LOCAL_DIR := $(call my-dir)

          PRODUCT_MAKEFILES := \
              $(LOCAL_DIR)/omni_gtactive3.mk \
              $(LOCAL_DIR)/twrp_gtactive3.mk

          COMMON_LUNCH_CHOICES := \
              omni_gtactive3-eng \
              omni_gtactive3-userdebug \
              twrp_gtactive3-eng \
              twrp_gtactive3-userdebug
          EOF

          # 4. CI Build-Fixes in BoardConfig.mk injizieren
          sed -i '/TARGET_NO_RECOVERY/d' "$DEV_DIR/BoardConfig.mk" 2>/dev/null || true
          sed -i '/BOARD_USES_RECOVERY_AS_BOOT/d' "$DEV_DIR/BoardConfig.mk" 2>/dev/null || true
          cat >> "$DEV_DIR/BoardConfig.mk" <<'EOF'

          TARGET_NO_RECOVERY := false
          BOARD_USES_RECOVERY_AS_BOOT := false
          EOF

          echo "=== AndroidProducts.mk ==="
          cat "$DEV_DIR/AndroidProducts.mk"

      - name: Prepare build environment
        run: |
          cd "$GITHUB_WORKSPACE/twrp"
          source build/envsetup.sh

          lunch twrp_gtactive3-eng || lunch omni_gtactive3-eng

          export TARGET_DEVICE=gtactive3

          echo "=== Build variables ==="
          echo "TARGET_DEVICE=$TARGET_DEVICE"
          echo "TARGET_PRODUCT=$TARGET_PRODUCT"
          echo "TARGET_BUILD_VARIANT=$TARGET_BUILD_VARIANT"
          echo "PRODUCT_DEVICE=$(get_build_var PRODUCT_DEVICE)"
          echo "PRODUCT_NAME=$(get_build_var PRODUCT_NAME)"
          echo "TARGET_NO_RECOVERY=$(get_build_var TARGET_NO_RECOVERY)"

      - name: Build TWRP recovery image
        run: |
          cd "$GITHUB_WORKSPACE/twrp"
          source build/envsetup.sh
          lunch twrp_gtactive3-eng || lunch omni_gtactive3-eng
          export TARGET_DEVICE=gtactive3

          rm -rf out/target/product/gtactive3/recovery.img
          rm -rf out/target/product/gtactive3/boot.img

          echo "=== Building recoveryimage ==="
          mka -j"$(nproc --all)" recoveryimage

          if [ ! -f out/target/product/gtactive3/recovery.img ]; then
            echo "::error::recovery.img was not generated."
            exit 1
          fi

      - name: Pack recovery image
        run: |
          cd "$GITHUB_WORKSPACE/twrp"

          mkdir -p "$GITHUB_WORKSPACE/artifacts"

          cp out/target/product/gtactive3/recovery.img \
             "$GITHUB_WORKSPACE/artifacts/twrp-gtactive3-recovery.img"

          tar -cvf "$GITHUB_WORKSPACE/artifacts/twrp-gtactive3.tar" \
            -C "$GITHUB_WORKSPACE/artifacts" \
            twrp-gtactive3-recovery.img

          sha256sum "$GITHUB_WORKSPACE/artifacts/"*

      - name: Upload TWRP artifacts
        uses: actions/upload-artifact@v4
        with:
          name: twrp-gtactive3
          path: artifacts/
          if-no-files-found: error
